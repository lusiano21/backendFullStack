<header>
   <nav class="navbar navbar-expand-lg bg-light">
            <div class="container-fluid">
              <a class="navbar-brand" href="#"><img src="../public/logo.jpg" alt="logo" style="height: 4rem;"></a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse " id="navbarNavAltMarkup">
                <div class="navbar-nav justify-content-around" style="width: 100%;">
                  <a class="nav-link active" aria-current="page" href="#">Home</a>
                  <a class="nav-link" href="#">Features</a>
                  <a class="nav-link" href="#">Pricing</a>
                  <a class="nav-link disabled">Disabled</a>
                </div>
              </div>
            </div>
          </nav>
</header>
<a href="{{sortLink}}">Calificación</a>

<div id="main" role="grid">
    {{#each payload}}
    <h1>Hola, {{this.nombre}} desde handlebars</h1>
    <li>{{this.nombre}}</li>
    <li>{{this.apellido}}</li>
    <li>{{this.edad}}</li>
    <li>{{this.email}}</li>
    {{/each}}
</div>
<p>Página actual: {{page}}</p>
{{#if prevLink}}
  <a href="{{prevLink}}" role="button">Anterior</a>
{{/if}}
{{#if nextLink}}
  <a href="{{nextLink}}" role="button">Siguente</a>
{{/if}}